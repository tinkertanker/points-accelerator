name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: pointsaccelerator
          POSTGRES_USER: pointsaccelerator
          POSTGRES_PASSWORD: pointsaccelerator
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pointsaccelerator"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://pointsaccelerator:pointsaccelerator@localhost:5432/pointsaccelerator
      ADMIN_TOKEN: ci-admin-token
      GUILD_ID: ci-guild
      VITE_API_BASE_URL: http://127.0.0.1:4173
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install
      - run: npm --workspace apps/backend run prisma:generate
      - run: npm --workspace apps/backend run prisma:migrate
      - run: npm run build
      - run: npm run test
